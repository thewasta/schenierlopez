---
// About Section Component - Corporate Tech Noir Design
---

<!-- About Section -->
<section id="about" class="relative min-h-screen py-20 overflow-hidden">
  <!-- Background Layers -->
  <div class="absolute inset-0">
    <!-- Gradient Background -->
    <div
      class="absolute inset-0 bg-gradient-to-b from-slate-900 via-slate-950 to-slate-900"
    >
    </div>

    <!-- Subtle Tech Pattern -->
    <div class="absolute inset-0 opacity-[0.02]">
      <div class="absolute inset-0 bg-tech-pattern-secondary"></div>
    </div>

    <!-- Floating Code Elements -->
    <div class="absolute inset-0 overflow-hidden pointer-events-none">
      <!-- Background Code 1 -->
      <div
        class="code-background-element absolute top-10 right-10 text-blue-400/20 font-mono text-xs"
      >
        <div>class Developer &#123;</div>
        <div>private $expertise = 7.years;</div>
        <div>private $passion = "APIs";</div>
        <div>public function __construct() &#123; &#125;</div>
        <div>&#125;</div>
      </div>

      <!-- Background Code 2 -->
      <div
        class="code-background-element absolute bottom-20 left-10 text-emerald-400/15 font-mono text-xs"
      >
        <div>// Backend Excellence</div>
        <div>
          const skills = {}
          <div>laravel: 'expert',</div>
          <div>nodejs: 'expert',</div>
          <div>architecture: 'advanced'</div>
          <div></div>
        </div>
      </div>
    </div>
  </div>

  <!-- Main Content -->
  <div class="relative z-10 container mx-auto px-6">
    <!-- Section Header -->
    <div class="text-center mb-12 md:mb-16 animate-reveal">
      <h2 class="text-5xl md:text-6xl font-bold font-display mb-6">
        <span
          class="text-transparent bg-clip-text bg-gradient-to-r from-blue-400 via-emerald-400 to-blue-500"
        >
          Sobre Mí
        </span>
      </h2>
      <p class="text-xl text-slate-300 max-w-3xl mx-auto leading-relaxed">
        Desarrollador Backend especializado en Symfony y Laravel con 6 años de
        experiencia construyendo APIs RESTful escalables con principios SOLID
      </p>
    </div>

    <!-- Main Content Grid -->
    <div
      class="grid lg:grid-cols-2 gap-12 lg:gap-16 items-center mb-12 lg:mb-20"
    >
      <!-- Professional Photo & Stats -->
      <div class="relative order-2 lg:order-1">
        <!-- Photo Container -->
        <div class="relative mb-8 group">
          <!-- Glow Effect -->
          <div
            class="absolute -inset-4 bg-gradient-to-r from-blue-600 to-emerald-600 rounded-2xl opacity-20 blur-xl group-hover:opacity-30 transition-opacity duration-300"
          >
          </div>

          <!-- Photo Frame -->
          <div
            class="relative bg-slate-800 rounded-2xl p-1 transform group-hover:scale-[1.02] transition-transform duration-500"
          >
            <!-- Profile Image Placeholder -->
            <div
              class="aspect-square bg-gradient-to-br from-slate-800 to-slate-900 rounded-xl flex items-center justify-center"
            >
              <!-- Profile Icon -->
              <div class="text-center">
                <div
                  class="w-32 h-32 mx-auto mb-4 rounded-full bg-gradient-to-br from-blue-500 to-emerald-500 flex items-center justify-center"
                >
                  <svg
                    class="w-16 h-16 text-white"
                    fill="currentColor"
                    viewBox="0 0 24 24"
                  >
                    <path
                      d="M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm0 3c1.66 0 3 1.34 3 3s-1.34 3-3 3-3-1.34-3-3 1.34-3 3-3zm0 14.2c-2.5 0-4.71-1.28-6-3.22.03-1.99 4-3.08 6-3.08 1.99 0 5.97 1.09 6 3.08-1.29 1.94-3.5 3.22-6 3.22z"
                    ></path>
                  </svg>
                </div>
                <div class="text-slate-400 font-display text-lg">
                  Schenier Lopez
                </div>
                <div class="text-slate-500 text-sm">Backend Developer</div>
              </div>
            </div>
          </div>

          <!-- Decorative Elements -->
          <div
            class="absolute -top-4 -right-4 w-20 h-20 bg-blue-500/20 rounded-full blur-xl"
          >
          </div>
          <div
            class="absolute -bottom-4 -left-4 w-24 h-24 bg-emerald-500/20 rounded-full blur-xl"
          >
          </div>
        </div>

        <!-- Key Stats -->
        <div class="grid grid-cols-3 gap-4">
          <div
            class="stat-card text-center p-4 bg-slate-800/60 backdrop-blur-sm rounded-xl border border-slate-700/50 hover:border-blue-500/30 transition-all duration-300"
          >
            <div
              class="text-3xl font-bold font-display text-blue-400 mb-1 counter"
              data-target="6"
            >
              0
            </div>
            <div class="text-slate-400 text-sm font-body">Años</div>
          </div>
          <!--<div
            class="stat-card text-center p-4 bg-slate-800/60 backdrop-blur-sm rounded-xl border border-slate-700/50 hover:border-emerald-500/30 transition-all duration-300"
          >
            <div
              class="text-3xl font-bold font-display text-emerald-400 mb-1 counter"
              data-target="50"
            >
              0
            </div>
            <div class="text-slate-400 text-sm font-body">APIs</div>
          </div> -->
          <div
            class="stat-card text-center p-4 bg-slate-800/60 backdrop-blur-sm rounded-xl border border-slate-700/50 hover:border-purple-500/30 transition-all duration-300"
          >
            <div
              class="text-3xl font-bold font-display text-purple-400 mb-1 counter"
              data-target="100"
            >
              0
            </div>
            <div class="text-slate-400 text-sm font-body">
              % Cliente Satisfecho
            </div>
          </div>
        </div>
      </div>

      <!-- Professional Narrative -->
      <div class="order-1 lg:order-2 space-y-8">
        <!-- Professional Title -->
        <div class="animate-reveal-delay-1">
          <h3
            class="text-3xl md:text-4xl font-bold font-display mb-4 text-slate-100"
          >
            Desarrollador Backend <span
              class="text-transparent bg-clip-text bg-gradient-to-r from-blue-400 to-emerald-400"
              >Symfony, Laravel, NestJS</span
            >
          </h3>
          <div
            class="w-24 h-1 bg-gradient-to-r from-blue-500 to-emerald-500 rounded-full mb-6"
          >
          </div>
        </div>

        <!-- Professional Summary -->
        <div
          class="space-y-4 text-slate-300 leading-relaxed animate-reveal-delay-2"
        >
          <p class="font-body">
            Desarrollador Backend con 6 años de experiencia especializado en
            Symfony, Doctrine y Laravel. Aplico Diseño de Dominio, Arquitectura
            Hexagonal y principios SOLID para construir APIs RESTful mantenibles
            y escalables.
          </p>
          <p class="font-body">
            Actualmente en The Cocktail desarrollando APIs para apps móviles en
            hostelería, con experiencia previa en e-commerce y proyectos
            freelance completos. Trabajo con metodologías ágiles, DDD y
            pipelines CI/CD en Azure.
          </p>
        </div>

        <!-- Core Philosophy -->
        <div
          class="bg-slate-800/40 backdrop-blur-sm rounded-xl p-6 border border-slate-700/50 animate-reveal-delay-3"
        >
          <h4 class="text-xl font-semibold font-display text-blue-400 mb-4">
            Enfoque de Desarrollo
          </h4>
          <div class="space-y-3">
            <div class="flex items-start space-x-3">
              <div
                class="w-6 h-6 rounded-full bg-emerald-500/20 flex items-center justify-center mt-0.5"
              >
                <div class="w-2 h-2 rounded-full bg-emerald-400"></div>
              </div>
              <div>
                <h5 class="font-semibold text-slate-100 mb-1">
                  Clean Architecture
                </h5>
                <p class="text-slate-400 text-sm font-body">
                  Aplico Arquitectura Hexagonal y principios SOLID para mantener
                  código desacoplado y mantenible
                </p>
              </div>
            </div>
            <div class="flex items-start space-x-3">
              <div
                class="w-6 h-6 rounded-full bg-blue-500/20 flex items-center justify-center mt-0.5"
              >
                <div class="w-2 h-2 rounded-full bg-blue-400"></div>
              </div>
              <div>
                <h5 class="font-semibold text-slate-100 mb-1">
                  Domain-Driven Development
                </h5>
                <p class="text-slate-400 text-sm font-body">
                  Aplico principios DDD para mantener código desacoplado y
                  mantenible
                </p>
              </div>
            </div>
            <div class="flex items-start space-x-3">
              <div
                class="w-6 h-6 rounded-full bg-purple-500/20 flex items-center justify-center mt-0.5"
              >
                <div class="w-2 h-2 rounded-full bg-purple-400"></div>
              </div>
              <div>
                <h5 class="font-semibold text-slate-100 mb-1">
                  Integraciones Complejas
                </h5>
                <p class="text-slate-400 text-sm font-body">
                  Experiencia con APIs REST/SOAP y mensajería asíncrona con
                  RabbitMQ para sistemas distribuidos
                </p>
              </div>
            </div>
          </div>
        </div>

        <!-- CTA -->
        <div class="animate-reveal-delay-4">
          <a href="#contact" class="btn-primary group">
            <span class="flex items-center justify-center">
              <svg
                class="w-5 h-5 mr-2 group-hover:rotate-12 transition-transform duration-300"
                fill="none"
                stroke="currentColor"
                viewBox="0 0 24 24"
              >
                <path
                  stroke-linecap="round"
                  stroke-linejoin="round"
                  stroke-width="2"
                  d="M12 4.354a4 4 0 110 5.292M15 21H3v-1a6 6 0 0112 0v1zm0 0h6v-1a6 6 0 00-9-5.197M13 7a4 4 0 11-8 0 4 4 0 018 0z"
                ></path>
              </svg>
              Contactar para Colaborar
            </span>
          </a>
        </div>
      </div>
    </div>
  </div>

  <!-- Custom Styles for This Component -->
  <style>
    /* Tech Pattern Background */
    .bg-tech-pattern-secondary {
      background-image: radial-gradient(
          circle at 80% 20%,
          rgba(16, 185, 129, 0.08) 0%,
          transparent 50%
        ),
        radial-gradient(
          circle at 20% 80%,
          rgba(59, 130, 246, 0.06) 0%,
          transparent 50%
        ),
        radial-gradient(
          circle at 60% 60%,
          rgba(139, 92, 246, 0.04) 0%,
          transparent 50%
        );
      background-size:
        300px 300px,
        400px 400px,
        350px 350px;
      background-position:
        100px 50px,
        200px 200px,
        50px 150px;
    }

    /* Background Code Elements */
    .code-background-element {
      opacity: 0;
      animation: fade-code-bg 1.5s ease-out forwards;
    }

    .code-background-element:nth-child(1) {
      animation-delay: 0.5s;
    }

    .code-background-element:nth-child(2) {
      animation-delay: 1s;
    }

    @keyframes fade-code-bg {
      from {
        opacity: 0;
        transform: translateY(20px);
      }
      to {
        opacity: 0.4; /* Kept subtle */
        transform: translateY(0);
      }
    }

    /* Animation Classes */
    .animate-reveal-delay-1 {
      opacity: 0;
      animation: fadeInUp 0.8s ease-out 0.3s forwards;
    }

    .animate-reveal-delay-2 {
      opacity: 0;
      animation: fadeInUp 0.8s ease-out 0.6s forwards;
    }

    .animate-reveal-delay-3 {
      opacity: 0;
      animation: fadeInUp 0.8s ease-out 0.9s forwards;
    }

    .animate-reveal-delay-4 {
      opacity: 0;
      animation: fadeInUp 0.8s ease-out 1.2s forwards;
    }

    .animate-slide-up-delay-1 {
      opacity: 0;
      transform: translateY(30px);
      animation: slideUp 0.8s ease-out 0.2s forwards;
    }

    .animate-slide-up-delay-2 {
      opacity: 0;
      transform: translateY(30px);
      animation: slideUp 0.8s ease-out 0.4s forwards;
    }

    @keyframes fadeInUp {
      from {
        opacity: 0;
        transform: translateY(20px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    @keyframes slideUp {
      from {
        opacity: 0;
        transform: translateY(30px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    /* Stats Counter Animation */
    .counter {
      font-variant-numeric: tabular-nums;
    }

    /* Skill Progress Animation */
    .skill-progress {
      width: 0%;
      transition: width 2s cubic-bezier(0.4, 0, 0.2, 1);
    }

    .skill-progress.animated {
      width: var(--width);
    }

    /* Hover Effects for Cards */
    .stat-card:hover {
      transform: translateY(-4px);
      box-shadow: 0 20px 40px rgba(0, 0, 0, 0.3);
    }

    /* Skill Items Hover */
    .skill-item:hover .skill-progress {
      filter: brightness(1.2);
    }

    /* Responsive Typography */
    @media (max-width: 768px) {
      .animate-reveal,
      .animate-reveal-delay-1,
      .animate-reveal-delay-2,
      .animate-reveal-delay-3,
      .animate-reveal-delay-4 {
        animation: none;
        opacity: 1;
      }

      .code-background-element {
        display: none;
      }
    }

    /* Reduced Motion Support */
    @media (prefers-reduced-motion: reduce) {
      .animate-reveal,
      .animate-reveal-delay-1,
      .animate-reveal-delay-2,
      .animate-reveal-delay-3,
      .animate-reveal-delay-4,
      .animate-slide-up,
      .animate-slide-up-delay-1,
      .animate-slide-up-delay-2 {
        animation: none;
        opacity: 1;
        transform: none;
      }

      .skill-progress {
        transition: none;
      }

      .code-background-element {
        animation: none;
        opacity: 0.3;
      }
    }
  </style>
</section>
